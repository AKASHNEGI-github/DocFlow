# Copy to .env.local for local overrides. Left unset, api/client.js
# defaults to '/api/v1' - same-origin, relying on vite.config.js's dev
# proxy to forward to the backend, so this file is optional in dev.
VITE_API_BASE_URL=/api/v1
